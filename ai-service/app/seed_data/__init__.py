# Unused. The demand forecast (app/routers/forecast.py) reads real booking
# history directly from Postgres (seeded once by backend/prisma/seed.ts)
# rather than generating its own synthetic data here — see the design-choice
# note at the top of forecast.py.
